<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_sortie_index') }}">Sortir.com</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_sortie_index') }}">Liste des sorties</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_sortie_creer') }}">Créer une sortie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_participant_voir_profil', {id: app.user.id}) }}">Voir mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>