{% extends 'base.html.twig' %}

{% block title %}Créer une sortie{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="container">
            <button type="submit" class="btn btn-success">Créer la sortie</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
